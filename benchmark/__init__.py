"""trident-vs-baseline agent benchmark.

Indexes a target repo with trident, then runs two Claude Code agents over the
same question — one with the trident skill enabled, one with stock tools —
and reports answers plus token / dollar cost.
"""
